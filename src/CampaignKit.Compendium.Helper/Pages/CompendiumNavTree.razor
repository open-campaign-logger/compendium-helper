<RadzenRow>
    <RadzenTextBox Placeholder="Search..." @oninput="OnSearchChanged" />

</RadzenRow>
<RadzenRow>
    <RadzenTree Data="@FilteredCompendiums" >
        <RadzenTreeLevel TextProperty="Title" ChildrenProperty="SourceDataSetGroupings" Expanded="@(node => true)"/>
        <RadzenTreeLevel TextProperty="LabelName" ChildrenProperty="SourceDataSets" Expanded="@(node => IsNodeExpanded(node))" />
        <RadzenTreeLevel TextProperty="SourceDataSetName" HasChildren="@((node) => false)" />
    </RadzenTree>
</RadzenRow>
